# Django + Docker + Postgres + Traefik + PgAdmin

Clone the repo & Run this command

`docker compose up -d `

You can browse the django, pgadmin & Traefik Dashboard

`django.localhost`

`pgadmin.localhost`

`django.localhost:8080`
